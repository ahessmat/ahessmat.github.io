
Do your work on the gh-pages branch:
git checkout gh-pages

When you are finished, commit your changes.
After commiting your changes merge your gh-pages branch to master:
git checkout master
git merge gh-pages
git commit -m "comment"
git push


To run locally, navigate to myblog and type:
jekyll serve

This will spin up a local site on:
localhost:4000
